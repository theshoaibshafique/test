import React from 'react';

const FooterText = () => (
  <div>
    <i>Based on <strong>65 days</strong> with elective hours from your filter criteria <br />
ORs with no data available are excluded from the report
    </i>
  </div>
);
export default FooterText;
